import { SxProps, Theme } from '@mui/material'

export const Figure: SxProps<Theme> = {
  border: 2,
  borderColor: 'primary.main',
  borderRadius: 2,
  p: 1,
  display: 'flex',
  img: {
    width: '100%'
  }
}
